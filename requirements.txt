# Fase 1 — ingestão e detecção
PyMuPDF>=1.24
rapidfuzz>=3.0
python-docx>=1.1        # ingestão de fontes .docx (ex: homebrew em Word)

# Google Drive (OAuth local, acesso somente leitura)
google-api-python-client>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Fase 2 — voz (STT local, offline após 1º download do modelo)
faster-whisper>=1.0        # transcrição local (CTranslate2)
webrtcvad-wheels>=2.0      # detecção de voz / segmentação (wheels prontas)
soundfile>=0.12            # leitura de WAV
numpy>=1.24
sounddevice>=0.4           # captura de microfone (requer PortAudio no SO)

# Fase 3 — página web ao vivo
fastapi>=0.110
uvicorn>=0.29
watchfiles>=0.21
websockets>=12.0
pydantic>=2.0

# Dev/testes
pytest>=8.0
ruff>=0.5
build>=1.2
